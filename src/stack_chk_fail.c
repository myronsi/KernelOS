/* Минимальная реализация функции защиты от переполнения стека.
 При обнаружении повреждения стека система переходит в бесконечный цикл. */
void __stack_chk_fail_local(void) {
    /* Здесь можно добавить вывод сообщения об ошибке или индикацию,
     но для минимального варианта просто зациклимся. */
    while (1) {
        // Можно добавить ассемблерное прерывание (hlt) для экономии энергии
        __asm__ __volatile__("hlt");
    }
}
