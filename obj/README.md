This directory stores all the object files that will be linked together.

packages to install 
sudo pacman -S --needed base-devel gcc-multilib glibc-multilib nasm grub xorriso make lib32-glibc lib32-gcc-libs
